# AGENTS.md - Workspace Policy

## Command Execution & Permission Policy
- For this workspace (`construction_management_app`), automatically execute terminal commands proactively when needed (build, test, python compilation, migrate, git status, etc.).
